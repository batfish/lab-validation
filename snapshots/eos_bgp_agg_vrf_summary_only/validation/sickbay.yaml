entries:
  # c2 (aggregator): the locally-generated aggregate carries weight 32768 in
  # Batfish but 0 on the device (Arista uses 0 for locally-originated paths).
  # The aggregate as_path now matches (batfish/batfish#10094 fixed): both show
  # the contributors' common leading AS_SEQUENCE, and c1/c3 no longer diverge.
  # This is the pre-existing local-weight difference only.
  # https://github.com/batfish/lab-validation/issues/152
  - hostname: c2
    test_name: test_bgp_rib_routes
    skip:
      reason: aggregate local weight 32768 vs 0, https://github.com/batfish/lab-validation/issues/152
